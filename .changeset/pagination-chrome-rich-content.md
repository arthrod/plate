---
"@platejs/pagination": minor
---

Support rich header/footer chrome content: `ChromeContent` accepts `html` (inline bold/italic, author-authored) in addition to plain `text`, and the chrome bands render it with the region's typography. Adds `hasChromeContent`.
