---
'@platejs/pagination': minor
---

Header/Footer toggle now inserts empty children instead of literal "Header"/"Footer" placeholder text — chrome regions look like Google Docs (blank, focusable) and DOCX export no longer carries the placeholder string into authored content.
