---
'@platejs/pagination': minor
---

Variant A page setup surface: configurable `pageNumber` slot (region/align/format/startAt/hideOnFirst), `firstPageDifferent` flag with new `firstPageHeader`/`firstPageFooter` element plugins, `setPageNumber` / `setFirstPageDifferent` / `setHeaderHeight` / `setFooterHeight` transforms, `ChromeShell` wrapper component (label + focus border + exit affordance for header/footer regions), and `PageSetupDialog` (full Page Setup form covering page-size presets, per-axis margins, chrome heights, footnote placement, first-page rule, and page-number config). `MarginsDialog` is kept as a backwards-compatible alias.
